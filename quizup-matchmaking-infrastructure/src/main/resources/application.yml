spring:
  config:
    import: optional:classpath:quizup-defaults.yml
  profiles:
    default: local
  application:
    name: quizup-matchmaking

  flyway:
    locations: classpath:db/migration/matchmaking

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

microservice:
  swagger:
    description: QuizUp Matchmaking Module - Lobby-based player matching
  websocket:
    enabled: true


